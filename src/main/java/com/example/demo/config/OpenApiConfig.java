@Configuration
@OpenAPIDefinition(
        info = @Info(
                title = "Dynamic Event Ticket Pricing Engine",
                version = "1.0",
                description = "REST APIs for dynamic pricing"
        )
)
public class OpenApiConfig {
}
